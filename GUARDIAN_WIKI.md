# 📖 Guardian Wiki: `dev_guardian`

> Auto-generated by **Agentic Dev Guardian** on 2026-08-11 05:00 UTC  
> Source: Live Memgraph AST graph for `/home/smayan/Desktop/Agentic/backend/src/dev_guardian`
<!-- guardian:commit=8577d53e99e1b2d0d8a3eacfff6f40d0d6a67fa7 -->

---

## 🗺️ Architectural Overview

_AI-narrated summary of inter-module relationships derived from Memgraph._

{"explanation":"The package is organized around a thin core layer that provides shared utilities, logging, and a central \"core\" module which other components import. Backend selection is mediated by a backend_factory that abstracts OpenAI, Anthropic, Groq and other LLM backends, making these adapters (openai_backend, anthropic_backend, groq_backend, openai_compatible) peripheral yet interchangeable. Graph‑related modules (graph, sre_graph, refactor_graph, vector_manager) form a second hub, handling knowledge representation, retrieval and vector storage, and are consumed by higher‑level services such as refactor_planner, hybrid_retriever, and incident_response. The CLI, blueprint_validator, and schema modules orchestrate user interaction and validation, while specialized utilities (audit, rate_limiter, retry, tracing) provide cross‑cutting concerns. Overall, the architecture follows a modular, plug‑in style where the core and backend_factory act as the primary dependency roots, and the graph subsystem serves as the central data‑flow hub for intelligence and automation features."}

---

## 🏛️ Object-Oriented Hierarchy

_AI-narrated structural analysis of inherited classes._

{"explanation":"The class hierarchy centers on a few core base types: `BaseModel` (used for data structures like ChatRequest, ChatResponse, ASTNode, ParseResult, PromptTemplate, etc.), `ResponseSchema` (a subclass of BaseModel that groups all response‑oriented DTOs such as BlueprintValidation, GatekeeperReport, and IncidentTriage), and `HarnessError` (the common ancestor for domain‑specific exceptions). Backend implementations (`LocalBackend`, `HuggingFaceBackend`, `OllamaBackend`) share a thin layer by inheriting from the internal `_OpenAICompatibleBackend`, while the abstract `LLMBackend` is defined as a `Protocol`, indicating a duck‑typed contract rather than concrete inheritance. Enumerations (`NodeType`, `EdgeType`) and settings (`GuardianSettings`) are isolated, and several TypedDicts (`IncidentState`, `GuardianState`, `RefactorState`, `AgentReport`) provide lightweight, structural typing. Overall the design is flat with shallow inheritance trees, using mix‑in‑style base classes (BaseModel, ResponseSchema, HarnessError) to enforce consistency across data models and errors, while protocols and TypedDicts keep the system loosely coupled."}

---

## 🔥 Top 5 Highest-Complexity Functions

_Ranked by outgoing `CALLS` edges (blast radius). These are the highest architectural risk points in the codebase._


**1. `run_audit`** — 32 outgoing calls  
`audit.py`


**2. `build_wiki`** — 24 outgoing calls  
`docs/wiki_builder.py`


**3. `index`** — 20 outgoing calls  
`cli.py`


**4. `evaluate`** — 18 outgoing calls  
`cli.py`


**5. `sandbox_reproducer_node`** — 15 outgoing calls  
`agents/sandbox_reproducer.py`


---

## 📊 Execution Trace Summaries

### `run_audit`

{"explanation":"The run_audit function orchestrates the audit pipeline by first loading and preprocessing file content, then rendering markdown and generating a synthetic diff. It invokes core subsystems such as the HybridRetriever for embedding and retrieval, the MemgraphClient for graph queries, and the AuditReport/Finding models for result aggregation. It delegates policy enforcement to gatekeeper_node and adversarial testing to redteam_node, each of which log activity, access state, and produce agent reports. Throughout, progress callbacks, logging, and severity assessment are interleaved, and the final report is assembled via _report and markdown rendering. Thus, run_audit serves as the central coordinator that integrates I/O, retrieval, graph analytics, policy checks, and reporting into a cohesive audit execution flow."}



### `build_wiki`

{
  "explanation": "The `build_wiki` routine orchestrates the documentation pipeline by first retrieving the repository head SHA via `_head_sha`, then loading source files with Path.read_text and preparing sections. It invokes the HybridRetriever for content enrichment, timestamps the output, and logs progress. Subsequently it calls the three structural explainers—`explain_call_graph`, `explain_class_hierarchy`, and `explain_module_dependencies`—each of which queries the graph database (mg.execute_query) and assembles formatted text. Finally, `build_wiki` aggregates the generated fragments, appends them to the wiki sections list, and records top‑complex nodes via `get_top_complex_nodes`. Overall, it acts as the central coordinator that integrates version control metadata, file I/O, retrieval, logging, and structural analysis to produce a cohesive wiki document."
}



### `index`

{"explanation":"The `index` function orchestrates a multi‑stage indexing pipeline. It first initializes parsing and database clients (ASTParser, MemgraphClient, QdrantCodeClient), then emits status messages via `_echo` and validates infrastructure with `_require_infra` (which may abort via `typer.Exit`). It proceeds to collect garbage, enumerate source files (`path.rglob`), and for each file parses the AST (`parser.parse_file`) while skipping irrelevant nodes (`parser._should_skip`). Parsed entities are upserted into Memgraph (`mg._upsert_node`, `mg._upsert_edge`) and indexed in Qdrant (`qd.ensure_collection`, `qd.ingest_nodes`) after computing embeddings (`predict_embedding_strategy`). Throughout, it logs progress (`logger.info`), sorts results, and finally may terminate with `typer.Exit`. Thus, `index` acts as the central coordinator linking the parsing, logging, memory management, and graph/vector‑store subsystems to build and persist the index."}



### `evaluate`

{"explanation":"The evaluate() function orchestrates a multi‑stage evaluation pipeline. It first ensures the runtime environment is prepared by invoking _require_infra(), which logs messages via _echo() and validates readiness with require_ready() before potentially exiting via typer.Exit. It then constructs the guardian graph by calling build_guardian_graph(), which assembles a StateGraph, adds nodes, edges, conditional edges, sets the entry point, compiles the graph, and logs its creation. Next, evaluate() creates a HybridRetriever, embeds nodes, and performs retrieval operations. It processes the diff file, parses lines with regex, extracts changed entities, and invokes the compiled graph with the retrieved context. Throughout, it logs progress and aggregates results using standard utilities (len, str, etc.). Thus, evaluate() acts as the central coordinator, linking infrastructure validation, graph construction, retrieval, and diff analysis subsystems into a cohesive execution flow."}



### `sandbox_reproducer_node`

{"explanation":"The sandbox_reproducer_node orchestrates the incident‑reproduction pipeline by first initializing a fresh GuardianState, then constructing a pseudo‑diff of the incident via _build_incident_pseudo_diff. It sequentially invokes gatekeeper_node and redteam_node, passing along shared context objects (state, report, result, incident_context) and logging progress. Throughout it aggregates reports (gk_report, rt_report) and results (gk_result, rt_result), copies the MOA state, and finally normalizes the verdict string. Thus, sandbox_reproducer_node acts as the top‑level coordinator that triggers the gatekeeper and red‑team subsystems, consolidates their outputs, and prepares the final reproducible incident payload."}



---

## 📝 Architectural Decision Records (ADRs)

_AI-narrated ADRs for the highest-complexity functions, grounded in the Memgraph structural context._


### ADR: `run_audit`

## Status
Accepted

## Context
{"context":"run_audit exists to orchestrate a synthetic audit of the most‑called functions in a repository, querying Memgraph for risk scores and invoking Gatekeeper and Red Team agents to generate fi

## Decision
{"context":"run_audit exists to orchestrate a synthetic audit of the most‑called functions in a repository, querying Memgraph for risk scores and invoking Gatekeeper and Red Team agents to generate fi

## Consequences



### ADR: `build_wiki`

## Status
Accepted

## Context
{"context":"The function builds a complete markdown wiki for a repository by orchestrating multiple documentation generators that consume pre‑indexed Memgraph AST data, avoiding any re‑parsing of sour

## Decision
{"context":"The function builds a complete markdown wiki for a repository by orchestrating multiple documentation generators that consume pre‑indexed Memgraph AST data, avoiding any re‑parsing of sour

## Consequences



### ADR: `index`

## Status
Accepted

## Context
{"context":"The `index` function provides a CLI entry point to parse a codebase with Tree‑sitter, stream each file’s AST into Memgraph, and optionally generate Qdrant vector embeddings, enabling repos

## Decision
{"context":"The `index` function provides a CLI entry point to parse a codebase with Tree‑sitter, stream each file’s AST into Memgraph, and optionally generate Qdrant vector embeddings, enabling repos

## Consequences



### ADR: `evaluate`

## Status
Draft

## Context
_ADR generation failed: Backend 'groq' unavailable: Error code: 400 - {'error': {'message': "Failed to generate JSON. Please adjust your prompt._


### ADR: `sandbox_reproducer_node`

## Status
Accepted

## Context
{"context":"The function acts as a LangGraph node that attempts to reproduce a reported bug by turning the incident context into a pseudo‑diff, retrieving relevant module context, and delegating valid

## Decision
{"context":"The function acts as a LangGraph node that attempts to reproduce a reported bug by turning the incident context into a pseudo‑diff, retrieving relevant module context, and delegating valid

## Consequences



---

_End of Guardian Wiki_
